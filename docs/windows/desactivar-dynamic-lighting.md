# Desactivar Dynamic Lighting

**Win+r**, luego `regedit`

`Computer\HKEY_CURRENT_USER\Software\Microsoft\Lighting`

Luego configure la clave dentro de la carpeta llamada `AmbientLightingEnabled` y establezca el valor en `0`.
